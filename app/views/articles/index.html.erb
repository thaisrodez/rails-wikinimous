<div class="container">
  <% @articles.each do |article| %>
    <div class="article-card">
      <h3><%= link_to article.title, article_path(article.id) %></h3>
      <p><%= article.content %></p>
    </div>
  <% end %>
</div>
